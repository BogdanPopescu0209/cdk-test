{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx esbuild src/index.ts --bundle --outfile=dist/index.js --platform=node --target=esnext"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.24.2",
    "sharp": "^0.33.5"
  }
}